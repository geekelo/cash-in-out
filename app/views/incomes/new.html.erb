<!-- app/views/incomes/new.html.erb -->
<div class="topbar">
  <button class="back_icon signout_icon" onclick="javascript:history.back()">❮</button>
  <p class="topbar_text">Top-up Credit</p>
  <button class="back_icon signout_icon" onclick="toggleMenu()">☰</button>
 </div>

   
  <div class="reglogo_cont"><%= image_tag 'reglogo.png', alt: 'reg logo', class: 'reglogo' %></div>
  <section class="forms">
<%= form_with(model: @income, local: true) do |form| %>
  <%= form.label :amount %>
  <%= form.number_field :amount %>


  <div class="bottombar">
    <%= form.submit "Proceed", class: 'bottombar_text' %>
  </div>
<% end %>
</section>
